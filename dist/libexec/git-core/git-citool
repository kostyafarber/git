#!/bin/sh
if test "z$*" = zversion ||
   test "z$*" = z--version
then
	echo 'git-gui version 0.21.GITGUI-dirty'
else
	libdir="${GIT_GUI_LIB_DIR:-/Users/kostyafarber/repos/git/dist/share/git-gui/lib}"
	exec "$libdir/Git Gui.app/Contents/MacOS/Wish" "$0" "$@"
fi
